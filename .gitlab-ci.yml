image: debian

before_script:
  - apt update && apt install -y openssh-client sshpass  # Instala os clientes SSH e sshpass

stages:
  - deploy

deploy_production:
  stage: deploy
  script:
    - echo "🚀 Conectando ao servidor de produção e rodando o script de deploy..."
    - sshpass -v -p $SSHPASS ssh -o "StrictHostKeyChecking no" $USERNAME@$SERVER_IP_PROD "cd $SERVER_PATH_PROD && bash ./rebuild_sisalmox.sh"
   
  only:
    - master
  environment:
    name: production
    url: http://localhost:3010/login

deploy_develop:
  stage: deploy
  script:
    - echo "🚀 Conectando ao servidor de desenvolvimento e rodando o script de deploy..."
    - sshpass -v -p $SSHPASS ssh -o "StrictHostKeyChecking no" $USERNAME@$SERVER_IP_DEV "cd $SERVER_PATH_DEV && bash ./rebuild_sisalmox.sh"
    
  only:
    - dev
  environment:
    name: development
    url: http://localhost:3010/login
